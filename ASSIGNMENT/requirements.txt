opencv-python-headless>=4.8
numpy>=1.24
scipy>=1.10
filterpy>=1.4.5
pandas>=2.0
matplotlib>=3.7
# Optional — enables real YOLO-based detection instead of the fallback
# contour-based detector in detector.py. Requires trained weights.
# ultralytics>=8.0
